# Repository workflow

After every successful Git commit, immediately push the current branch to
`origin`. Do not leave local-only commits. Confirm that the push succeeded
before reporting the commit as complete.
